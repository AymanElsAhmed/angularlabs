import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Product } from '../app/getproduct.model';

@Injectable({
  providedIn: 'root',
})
export class GetproductsService {
  constructor() {}
}
